import { itemConstants } from '../constants';

const baseState = {

};

const items = (state = baseState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default items;
